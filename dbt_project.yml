name: 'Airflow_dbt'
version: '1.0.0'
config-version: 2


profile: 'default'

model-paths: ["models"]

target-path: "target"

models: 
  airflow_dbt:
    example:
      materialized: view